# composer — sourced from composer.lock. The namespaced name renders with an
# encoded slash (psr%2Flog) and currently yields no synthesized CPE, so only the
# purl is asserted here.
purl pkg:composer/psr%2Flog@3.0.0
