# The luarocks extractor derives the package name from the install-tree layout
# (rocks-<lua>/<name>/<version>/...), not the rockspec body, so the fixture must
# live under rocks-5.4/mylib/1.0-1/ for the extractor to fire.
mylib
